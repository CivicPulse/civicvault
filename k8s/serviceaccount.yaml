apiVersion: v1
kind: ServiceAccount
metadata:
  name: civicvault
  namespace: civicvault
  labels:
    app.kubernetes.io/name: civicvault
# The app never talks to the Kubernetes API, so don't mount a token (least
# privilege — a leaked pod can't enumerate or mutate cluster resources).
automountServiceAccountToken: false
